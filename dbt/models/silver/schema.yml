version: 2
sources:
    - name: source
      database: MYDB
      schema: BRONZE
      tables:
        - name: WORK_PRODUCT_COPY
    - name: xfm
      database: MYDB
      schema: SILVER
      tables:
        - name: WORK_PRODUCT_TRANSFORM